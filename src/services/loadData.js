import employers from '../mocks/employers';
import header from '../mocks/header';
import basket from '../mocks/basket';
import texts from "../mocks/texts";

export const loadEmployers = () => {
  return employers;
};

export const loadHeader = () => {
  return header;
};

export const loadBasket = () => {
  return basket;
};

export const loadTexts = () => {
  return texts;
};
